import * as SQLite from 'expo-sqlite';




/* When handling these in your code, use a TypeScript type guard:

function isEpisode(item: TraktHistoryItem): item is TraktHistoryEpisode {
  return item.type === 'episode';
}
That way you can safely access episode and show fields only if it's an episode.

*/
